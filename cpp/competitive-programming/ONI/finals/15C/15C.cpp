#include <iostream>
#include <vector>
#include <map>
using namespace std;

int main()
{
    int N, P, i, c, s;
    cin >> N;
    vector<int> tree[N-1];

    for (i = 0; i < N-1; i++)
    {
        cin >> c >> s;
        tree[c].push_back(s);
    }

    cin >> P;

    for (i = 0; i < P; i++)
    {
        cin >> c >> s;
        vector<int[N-1]> tabela;
    }

    return 0;
}
