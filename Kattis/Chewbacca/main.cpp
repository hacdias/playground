#include <bits/stdc++.h>
using namespace std;

typedef long long int lld;

lld n, k, q;

lld ancestor(lld x){
    return (x + k - 2) / k;
}

int main() {
  scanf("%lld %lld %lld", &n, &k, &q);


  return 0;
}
